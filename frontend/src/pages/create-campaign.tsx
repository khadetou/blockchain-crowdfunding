import Layout from "@/components/Layout";
import CreateCampaign from "@/components/screens/create-campaign";

const createCampaign = () => {
  return (
    <Layout>
      <CreateCampaign />
    </Layout>
  );
};

export default createCampaign;
